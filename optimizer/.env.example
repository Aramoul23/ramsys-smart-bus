# Flask session encryption key — change this in production
FLASK_SECRET_KEY=your_secret_key

# Shared secret key for receiving route sync from App 1
# Must match SYNC_SECRET_KEY in App 1's .env
SYNC_SECRET_KEY=change_this_to_random_string
